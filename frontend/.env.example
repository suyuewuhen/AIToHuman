# 前端（Vite）构建期变量示例：复制成 .env.local 后按需修改。
#
# 默认**全部留空**：前端与 API 同源，所有请求都走相对路径（开发时 Vite 代理 /api 与 /hubs，
# 部署时由同一个域名的反向代理转发）。这也是本地开发与默认部署的形态，通常不需要动这个文件。
#
# 只有在“前端与 API 不同源”时才需要填下面的值，例如运营后台放到自己的域名/子域：
#   app.example.com（主应用）+ ops.example.com（运营后台）+ api.example.com（后端）
# 对应的后端配置（少了这一项浏览器会在预检阶段拦掉请求）：
#   Cors__AllowedOrigins=https://ops.example.com,https://app.example.com

# API 基地址，例如 https://api.example.com；留空 = 同源相对路径。
VITE_API_BASE_URL=

# SignalR Hub 基地址；留空时跟随 VITE_API_BASE_URL。
VITE_HUB_BASE_URL=

# 运营后台顶栏“← 返回任务工作台”的目标地址，例如 https://app.example.com/
VITE_APP_HOME_URL=

# 主应用顶栏“运营后台 ↗”入口的目标地址，例如 https://ops.example.com/
# 本地开发不用填：运营后台就在同源的 /ops/（也可以直接访问 /ops.html）。
VITE_OPS_HOME_URL=
